= simple_form_for @move, :html => { :class => 'form-horizontal' } do |f|
  = f.association :move_type
  = f.input :title
  = f.input :body
  = f.association :user
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), moves_path, :class => 'btn'
