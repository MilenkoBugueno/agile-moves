-if (@tomato.state == 2 || @tomato.state == 4) && !@tomato.rating.present?
  .panel.panel-default
    .panel-heading
      %h3.panel-title= t("tomatoes.results")
    .panel-body
      /mark as done button
      = simple_form_for @move, :html => { :class => 'form-horizontal' } do |f|
        = f.input :results, :as => :ckeditor, label: false
        = f.button :submit
