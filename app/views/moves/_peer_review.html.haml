.panel.panel-default
  .panel-heading
    %h3.panel-title= t("moves.peer_review")
  .panel-body
    -if @move.registration.present?
      -@move.registration.users.each do |user|
        =user.name +  "; "
