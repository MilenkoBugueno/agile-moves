.panel.panel-default
  .panel-heading
    %h4.panel-title= t("moves.peer_review")
  .panel-body
    -if @move.registration.present?
      -@move.registration.users.each do |user|
        =user.name +  "; "
