- if @project.present?
  - page = controller.action_name
  .row
    .col-md-1
    .col-md-5
      %ol.breadcrumb
        %li
          =@project.title
        %li
          - if page == "work"
            =t("projects.work")
          - elsif  page == "report"
            =t("projects.report")
          - elsif  page == "plan"
            =t("projects.plan")
          - elsif  page == "certificate"
            =t("projects.certificate")
          -else
            = page
        %li.active
          -if @move_type.present?
            = @move_type.title
          -else
            ="All"

    .col-md-3
      -if @move_type.present?
        = link_to t("projects.newmove"), new_move_path(project_id: @project.id, move_type: @move_type.id), :class => 'btn btn-primary', :type => "button"
      -else
        = link_to t("projects.newmove"), new_move_path(project_id: @project.id), :class => 'btn btn-primary', :type => "button"
    .col-md-3
      - if page == "work"
        .btn-group
          = link_to t("projects.plan"), plan_projects_path(id: @project.id, date: @date, move_type: @move_type.id), :class => "btn btn-default" if has_plan_view(@project)
          = link_to t("projects.work"), work_projects_path(id: @project.id, date: @date, move_type: @move_type.id), :class => "btn btn-default active"
          = link_to t("projects.report"), report_projects_path(id: @project.id, move_type: @move_type.id), :class=> "btn btn-default"
          = link_to t("projects.certificate"), certificate_projects_path(id: @project.id, move_type: @move_type.id), :class=> "btn btn-default"

      -elsif page == "report"
        .btn-group
          = link_to t("projects.plan"), plan_projects_path(id: @project.id, date: @date, move_type: @move_type.id), :class => "btn btn-default" if has_plan_view(@project)
          = link_to t("projects.work"), work_projects_path(id: @project.id, date: @date, move_type: @move_type.id), :class => "btn btn-default"
          = link_to t("projects.report"), report_projects_path(id: @project.id, move_type: @move_type.id), :class=> "btn btn-default active"
          = link_to t("projects.certificate"), certificate_projects_path(id: @project.id, move_type: @move_type.id), :class=> "btn btn-default"
      -elsif page == "plan"
        .btn-group
          = link_to t("projects.plan"), plan_projects_path(id: @project.id, date: @date, move_type: @move_type.id), :class => "btn btn-default active" if has_plan_view(@project)
          = link_to t("projects.work"), work_projects_path(id: @project.id, date: @date, move_type: @move_type.id), :class => "btn btn-default"
          = link_to t("projects.report"), report_projects_path(id: @project.id, move_type: @move_type.id), :class=> "btn btn-default"
          = link_to t("projects.certificate"), certificate_projects_path(id: @project.id, move_type: @move_type.id), :class=> "btn btn-default"
      -elsif page == "certificate"
        .btn-group
          = link_to t("projects.plan"), plan_projects_path(id: @project.id, date: @date), :class => "btn btn-default" if has_plan_view(@project)
          = link_to t("projects.work"), work_projects_path(id: @project.id, date: @date), :class => "btn btn-default"
          = link_to t("projects.report"), report_projects_path(id: @project.id), :class=> "btn btn-default"
          = link_to t("projects.certificate"), certificate_projects_path(id: @project.id), :class=> "btn btn-default active"


